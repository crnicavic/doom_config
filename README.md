# doom_config

## dependencies

use pandoc with mathjax for live preview:

``` bash
sudo apt install pandoc mathjax
```

use math-preview from npm for live preview package:

``` bash
sudo npm install -g git+https://gitlab.com/matsievskiysv/math-preview
```


